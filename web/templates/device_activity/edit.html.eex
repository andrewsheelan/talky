<h2>Edit device activity</h2>

<%= render "form.html", changeset: @changeset,
                        action: device_activity_path(@conn, :update, @device_activity) %>

<%= link "Back", to: device_activity_path(@conn, :index) %>
